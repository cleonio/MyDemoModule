spring:
  application:
    name: config-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/KaitoKite/spring-cloud-config #配置文件所在仓库
          username:  KaitoKite
          password: 995809969xy
          default-label: master #配置文件分支
          search-paths: config  #配置文件所在根目录