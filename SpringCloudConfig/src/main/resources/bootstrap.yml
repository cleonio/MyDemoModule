spring:
  application:
    name: config-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/jimmy-xiong/config #配置文件所在仓库
          username:  879889240@qq.com
          password: 995809969xy
          default-label: master #配置文件分支
          search-paths: config-center  #配置文件所在根目录